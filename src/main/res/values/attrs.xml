<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->

    <declare-styleable name="NavBar">
        <attr name="android:text"/>
        <attr name="android:color"/>
        <attr name="android:textColor"/>
        <attr name="android:drawableLeft"/>
    </declare-styleable>

    <declare-styleable name="PieView">
        <attr name="android:color"/>
        <attr name="pieStrokeWidth" format="dimension"/>
    </declare-styleable>

    <attr name="YesNoDialogPositiveButton" format="reference"/>
    <attr name="YesNoDialogOuter" format="reference"/>
    <attr name="YesNoDialogMessage" format="reference"/>

    <attr name="NumberPickerButton" format="reference"/>
    <attr name="NumberPickerEditText" format="reference"/>

    <attr name="NavBarBase" format="reference"/>
    <attr name="NavBarBack" format="reference"/>
    <attr name="NavBarTitle" format="reference"/>
</resources>
